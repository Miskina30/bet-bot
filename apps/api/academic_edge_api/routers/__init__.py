"""API routers for every ``/v1`` endpoint (registered by ``main.create_app``).

Modules:
``catalog``  -- health, sources, events, markets, quotes.
``insights`` -- opportunities, resolver review, predictions, model metrics,
alerts and the paper ledger.
``stream``   -- Server-Sent Events.
"""
